{
  "gopath": {
    "title": "GOPATH",
    "description": "Specifies the GOPATH to use when no environment variable is set.",
    "type": "string",
    "default": "",
    "order": 1
  },
  "goroot": {
    "title": "GOROOT",
    "description": "Specifies the GOROOT to use when no environment variable is set.",
    "type": "string",
    "default": "",
    "order": 2
  },
  "maxConcurrency": {
    "title": "Max concurrency",
    "description": "Specifies the maximum number of concurrent running go tools.",
    "type": "number",
    "default": 2,
    "order": 3
  }
}
